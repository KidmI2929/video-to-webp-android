# Video to WebP Android

Android app project for converting a selected video to animated WebP.

## Build APK
Open the **Actions** tab, select **Build Android APK**, then tap **Run workflow**. When the run finishes, download the **VideoToWebP-debug-apk** artifact.

> Initial scaffold. The app UI is buildable; the production video/WebP conversion engine will be added separately.
